## Your {{ data.digest.period }} alert digest

You had **{{ total_activities }}** {{ total_activities | pluralize: "alert", "alerts" }} in your {{ data.digest.period }} digest:

{% for activity in activities %}

<blockquote>
{{ activity.data.alert.title | default: activity.event }} — {{ activity.inserted_at | date: "%b %-d, %-I:%M %p" }}
</blockquote>
{% endfor %}
