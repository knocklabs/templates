## Your {{ data.digest.period }} alert digest

You had **{{ total_activities }}** {{ total_activities | pluralize: "alert", "alerts" }} in the last 24 hours:

{% for activity in activities %}

<blockquote>
{{ activity.data.alert.title | default: activity.event }} — {{ activity.inserted_at }}
</blockquote>
{% endfor %}
